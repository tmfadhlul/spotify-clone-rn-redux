import { compose } from 'recompose'

import { WelcomeScreen } from './screens'

export default compose(
    
)(WelcomeScreen)

// export const loginEnhance = compose(

// )(LoginScreen)

// export const registerEnhance = compose(

// )(RegisterScreen)